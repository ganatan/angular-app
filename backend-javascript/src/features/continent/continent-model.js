class Continent {

  constructor(id, code, name, wikipediaLink,
    area, population, countriesNumber) {

    this.id = id;
    this.code = code;
    this.name = name;
    this.wikipediaLink = wikipediaLink;
    this.area = area;
    this.population = population;
    this.countriesNumber = countriesNumber;

  }
}

export default Continent;
