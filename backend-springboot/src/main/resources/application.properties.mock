# === Infos ===
app.version=1.0.0
app.name=backend-springboot

# === Database Client (mock | pg | mysql) ===
db.client=mock

# === Application ===
spring.application.name=backend-springboot
server.port=3003