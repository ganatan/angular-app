{
  "name": "examples-typescript",
  "version": "1.0.0",
  "description": "npm init",
  "main": "index.js",
  "scripts": {
    "dev-await": "tsc src/ts-await.ts  --outDir dist && node dist/ts-await",
    "dev-class": "tsc src/ts-class.ts  --outDir dist && node dist/ts-class",
    "dev-filter": "tsc src/ts-filter.ts  --outDir dist && node dist/ts-filter",
    "dev-for": "tsc src/ts-for.ts  --outDir dist && node dist/ts-for",
    "dev-push": "tsc src/ts-push.ts  --outDir dist && node dist/ts-push",
    "dev-remove-space": "tsc src/ts-remove-space.ts  --outDir dist && node dist/ts-remove-space",
    "dev-replace": "tsc src/ts-replace.ts  --outDir dist && node dist/ts-replace"
  },
  "author": "danny",
  "license": "ISC",
  "dependencies": {
    "express": "4.18.2"
  }
}
