describe('Continents Routes', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('GET /continents', () => {
    test('should return all continents', async () => {
      expect(true).toBe(true);
    });
  });

});
