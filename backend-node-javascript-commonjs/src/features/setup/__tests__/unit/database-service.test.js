'use strict';

describe('Database Service', () => {

  beforeEach(() => {
  });

  afterEach(() => {
    jest.clearAllMocks();
  });

  describe('createDatabase', () => {
    test('createDatabase', async () => {
      expect(true).toBe(true);
    });
  });

});
